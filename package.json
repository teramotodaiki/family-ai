{
  "name": "family-ai",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "dev:main": "turbo run dev --filter=main",
    "build": "turbo run build",
    "clean": "turbo run clean",
    "e2e:main": "maestro test -e APP_ID=com.familyai.main apps/main/maestro/flows/chat-send.yaml"
  },
  "devDependencies": {
    "turbo": "2.5.5"
  },
  "packageManager": "npm@10.0.0"
}
